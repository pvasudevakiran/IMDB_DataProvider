# IMDb Data Provider - example configuration
# Copy to .env and adjust as needed.

# Paths (relative to project root or absolute)
DATA_DIR=./data
LOG_DIR=./logs

# IMDb dataset source
IMDB_BASE_URL=https://datasets.imdbws.com/
ENABLE_AKAS=true

# Nightly update time (for documentation; actual schedule is cron/systemd)
UPDATE_TIME=22:00
UPDATE_TZ=Asia/Kolkata

# API
MAX_SEARCH_LIMIT=100

# In-process cache (cachetools)
CACHE_TITLE_TTL=21600
CACHE_TITLE_MAX=50000
CACHE_PERSON_TTL=21600
CACHE_PERSON_MAX=50000
CACHE_SEARCH_TTL=1800
CACHE_SEARCH_MAX=20000

# Multi-worker: ingest lock and version watcher
INGEST_LOCK_STALE_SECONDS=21600
VERSION_POLL_INTERVAL=15

# Optional: Redis cache (leave unset for in-process only)
# REDIS_URL=redis://localhost:6379/0
# REDIS_KEY_PREFIX=imdb:

# Docker Compose (optional)
# PORT=8000
# PUID=1000
# PGID=1000
# For separate volume layout, Compose sets: DOWNLOAD_DIR, TMP_DIR, PARQUET_DIR, META_DIR, LOG_DIR
